<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width" />
        <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link
            href="https://fonts.googleapis.com/css2?family=Rosario:ital,wght@0,300..700;1,300..700&display=swap"
            rel="stylesheet"
        />
        <meta name="generator" content={Astro.generator} />
        <title>PORTFOLIO WEB</title>
    </head>
    <body>
        <div class="background-container">
            <div class="central-light"></div>
        </div>
        <div class="content">
            <slot />
        </div>
    </body>
</html>

<style is:global>
    html {
        font-family: "Rosario", sans-serif;
        background: #000000;
        color: #fff;
        margin: 0;
        padding: 0;
        min-height: 100vh;
    }

    body {
        margin: 0;
        padding: 0;
        min-height: 100vh;
        position: relative;
        overflow-x: hidden;
    }

    .background-container {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        z-index: -1;
        background: #000000;
    }

    .central-light {
        position: absolute;
        bottom: 0;
        left: 50%;
        transform: translateX(-50%);
        width: 80%;
        height: 100%;
        background: radial-gradient(
            ellipse 50% 190% at 50% 90%,
            #ad381a 0%,
            #a52208 15%,
            #341616 35%,
            rgba(52, 22, 22, 0.3) 55%,
            rgba(52, 22, 22, 0.1) 75%,
            transparent 100%
        );
        filter: blur(40px);
        opacity: 0.8;
        animation: parabolicPulse 4s ease-in-out infinite;
    }

    @keyframes parabolicPulse {
        0%,
        100% {
            opacity: 0.8;
            filter: blur(40px) brightness(1);
        }
        25% {
            opacity: 0.9;
            filter: blur(35px) brightness(1.2);
        }
        50% {
            opacity: 1;
            filter: blur(30px) brightness(1.4) hue-rotate(10deg);
        }
        75% {
            opacity: 0.9;
            filter: blur(35px) brightness(1.2);
        }
    }

    .content {
        position: relative;
        z-index: 1;
        min-height: 100vh;
        padding: 0 2rem 2rem 2rem;
    }

    @media (max-width: 768px) {
        .central-light {
            width: 90%;
            background: radial-gradient(
                ellipse 60% 175% at 50% 100%,
                #ad381a 0%,
                #a52208 15%,
                #341616 35%,
                rgba(52, 22, 22, 0.3) 55%,
                transparent 85%
            );
            filter: blur(35px);
        }
    }

    @media (max-width: 480px) {
        .central-light {
            width: 95%;
            background: radial-gradient(
                ellipse 65% 10% at 50% 100%,
                #ad381a 0%,
                #a52208 20%,
                #341616 40%,
                rgba(52, 22, 22, 0.2) 60%,
                transparent 80%
            );
            filter: blur(30px);
        }
    }
</style>
